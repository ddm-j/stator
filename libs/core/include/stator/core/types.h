// types.h
#pragma once

#include <cstddef>
#include <limits>

namespace stator::core {
    // Vocabulary and Type Aliases

    // Numeric
    using real = double;
    using idx = std::size_t;
    using size = std::size_t;

}